# ER 사용 설명서

클릭해도 반응이 없는 버튼과 에러를 자동으로 찾아내는 도구입니다.
QA에 넘기기 전에 개발자가 스스로 한 번 돌려보는 용도로 만들었습니다.

---

## 1. 이 도구로 할 수 있는 것, 없는 것

### 잡아내는 것

| 판정 | 무엇인가 | 예 |
|---|---|---|
| **에러** | 클릭했을 때 JS 예외가 터지거나 서버가 4xx·5xx로 응답 | 핸들러에서 undefined 참조, API가 500 반환 |
| **무감** | 클릭은 되는데 화면·서버 요청·주소 어느 것도 바뀌지 않음 | 핸들러 연결 누락, 조건에 걸려 아무것도 안 함 |

**무감이 이 도구의 핵심입니다.** 에러 로그조차 남지 않아서 모니터링 도구로는 안 잡히고,
사람이 일일이 눌러봐야만 발견되는 종류의 결함입니다.

### 잡아내지 못하는 것

> **반응은 하는데 잘못 반응하는 결함은 못 잡습니다.**

저장 버튼이 엉뚱한 값을 저장해도, 화면이 바뀌기는 했으므로 **정상으로 판정**합니다.
이 도구는 "이거 한 번이라도 눌러봤나?" 수준의 최소 확인이고, 그 위는 사람이 하는 QA 영역입니다.

그 밖에 판정하지 않는 것:
- 비즈니스 로직이 정책에 맞는지 (기대값을 정의해야 판정 가능)
- 화면 디자인이 시안과 같은지
- 성능·부하

### 판정 기준은 요소이지 이름이 아닙니다

화면에서 `button`, `a`, `role=button`, `onclick` 에 해당하는 **요소를 기계적으로 전부** 수집한 뒤,
클릭하고 **관찰 가능한 변화가 있었는지**만 봅니다. 버튼 이름이나 기획 의도는 보지 않습니다.

관찰하는 신호는 다섯 가지입니다.

| 신호 | 확인하는 것 |
|---|---|
| 화면 구조 | DOM이 바뀌었는가 (MutationObserver) |
| 서버 요청 | 네트워크 요청이 나갔는가 |
| 주소 이동 | URL이 바뀌었는가 |
| 화면 표시 | 모달·토스트가 떴는가, 문서 높이·포커스가 바뀌었는가 |
| 스크롤 | 화면이 움직였는가 (앵커 링크, "맨 위로" 버튼) |
| 새 창 | 새 창이 열렸는가 (결제창 등) |
| JS 예외 | 예외가 발생했는가 |

하나라도 변하면 **정상**, 전부 무변화면 **무감**, 예외나 4xx·5xx면 **에러**입니다.

버튼 이름을 보는 곳은 **제외 규칙 한 군데뿐**입니다. `삭제`, `결제` 같은 단어가 들어간 버튼은
눌리면 곤란하므로 **클릭하지 않습니다**. 판정이 아니라 안전장치입니다.

---

## 2. 처음 한 번만 하는 준비

Node.js 20 이상이 필요합니다. ([nodejs.org](https://nodejs.org) 에서 LTS 버전 설치)

```bash
git clone <저장소 주소>
cd ER
npm install
```

`npm install` 이 끝나면 검사용 브라우저(Chromium)까지 자동으로 설치됩니다. 처음 한 번은 1~2분 걸립니다.

설치가 제대로 됐는지 확인하려면:

```bash
npm test
```

13개 항목이 모두 통과하면 준비가 끝난 것입니다. 이 테스트는 판정이 정확한지도 함께 검증합니다.

---

## 3. 사용법

### 웹 화면으로 쓰기

```bash
npm start
```

실행하면 터미널에 접속 주소가 표시됩니다.

```
  내 PC에서      →  http://localhost:3000
  같은 사내망에서 →  http://192.168.0.15:3000
```

브라우저로 `http://localhost:3000` 을 열고,

1. 왼쪽 **프로젝트** 목록에서 검사할 프로젝트를 고릅니다.
   처음에는 `빠른 검사` 하나만 있습니다. 주소만 넣고 바로 돌려볼 때 쓰는 기본 프로젝트입니다.
   자주 검사하는 시스템이 있다면 프로젝트로 따로 만들어 두세요 ([6장](#6-프로젝트)).
2. **검사할 주소**에 검사 대상 URL을 넣습니다. 개발 서버, 사내 주소, `localhost` 모두 됩니다.
   프로젝트에 등록한 주소가 있으면 입력칸 아래 `등록된 주소` 를 눌러 고를 수 있습니다.
3. **검사 범위**를 고릅니다.
   - `이 페이지만` — 입력한 주소 한 장만 검사합니다. 가장 빠릅니다.
   - `하위 경로까지` — 예를 들어 `/admin/product` 를 넣으면 `/admin/` 아래 화면들을 따라갑니다.
   - `사이트 전체` — 같은 도메인의 연결된 화면을 모두 따라갑니다. 오래 걸립니다.
4. 입력칸 아래 요약 줄에서 **어떤 로그인·규칙으로 검사하는지** 확인하고 **검사 시작**을 누릅니다.

검사 중에는 눈금이 하나씩 채워집니다. 색이 곧 판정이라 진행 상황과 결과를 동시에 볼 수 있습니다.
오래 걸린다 싶으면 **중단**을 눌러 그때까지의 결과만 볼 수도 있습니다.

### 검사 시간 어림잡기

요소 하나당 최대 2초입니다. 반응이 빨리 오면 더 빨리 넘어갑니다.

| 요소 수 | 대략 걸리는 시간 |
|---|---|
| 20개 | 30초 내외 |
| 100개 | 2~3분 |
| 500개 | 10분 이상 |

처음에는 `이 페이지만` 으로 한 화면씩 돌려보고, 익숙해지면 범위를 넓히시길 권합니다.

---

## 4. 함께 쓰기

ER은 **검사 엔진이 실행되는 컴퓨터의 네트워크로** 대상에 접속합니다.
그래서 **어디서 실행하느냐가 곧 검사 가능 범위**입니다.

| 실행 위치 | 사내·개발 주소 검사 | 언제 |
|---|:---:|---|
| 사내 서버 | 가능 | **최종 형태.** 24시간 떠 있고 팀 전체가 씁니다 |
| 내 컴퓨터 | 가능 | 서버를 받기 전, 혼자 쓰거나 잠깐 공유할 때 |
| 외부 클라우드 | **불가** | 사내망 밖이라 주소에 닿지 못합니다 |

### 사내 서버에 올리기

`Dockerfile` 이 들어 있어 명령 두 줄이면 됩니다.

```bash
docker build -t er .
docker run -d -p 3000:3000 --restart always er
```

인프라 담당자에게 요청할 사양:

- 사내망 **안에 있는** 리눅스 서버 1대
- 메모리 **2GB 이상** (브라우저를 띄우는 도구라 넉넉해야 합니다)
- 3000번 포트

올라가면 팀원은 `http://서버주소:3000` 으로 접속해 씁니다.
프로젝트·검사 기록·저장된 로그인은 **모두 서버에 남아 접속한 모든 사람이 공유**합니다.

> ⚠️ **서버에서는 "직접 로그인" 방식을 쓸 수 없습니다.**
> 로그인 창이 서버 화면에 뜨는데 볼 수 있는 사람이 없기 때문입니다.
> ER이 이를 감지해 그 방법을 막고 안내합니다. 로그인이 필요하면 아이디·비밀번호 방식을 쓰세요.
> 꼭 세션이 필요하면, 화면이 있는 컴퓨터에서 ER을 실행해 로그인 상태를 만든 뒤
> `data/sessions/<프로젝트ID>.json` 파일을 서버의 같은 위치로 옮기면 됩니다.
> 두 컴퓨터에 같은 프로젝트가 있어야 하므로 `data/projects.json` 도 함께 옮기세요.

### 내 컴퓨터에서 실행할 때

서버를 받기 전에는 각자 실행해도 됩니다.

1. `npm start` 를 실행해 둡니다.
2. 화면에 표시된 주소(예: `http://192.168.0.15:3000`)를 동료에게 알려줍니다.
3. 같은 사내망에 있는 동료는 그 주소로 접속하면 됩니다.

**내 컴퓨터가 꺼지면 같이 멈춥니다.** 절전 모드도 마찬가지고요.
그래서 오래 쓸 거라면 사내 서버로 올리는 편이 낫습니다.

### 접속이 안 될 때

| 증상 | 원인 | 해결 |
|---|---|---|
| 아무도 접속 못 함 | 사내 WiFi가 단말 간 통신을 막아둠 | 유선 연결로 시도하거나 인프라 담당자에게 문의 |
| 특정 사람만 안 됨 | 게스트 WiFi 등 다른 네트워크에 접속 중 | 같은 사내망으로 옮기기 |
| `.local` 주소가 안 됨 | 윈도우는 이 방식을 지원하지 않는 경우가 있음 | IP 주소(`192.168.x.x`) 형태로 알려주기 |
| 어제 되던 주소가 오늘 안 됨 | IP가 자동 할당이라 바뀜 | 화면에서 현재 주소를 다시 확인 (서버는 고정 IP 권장) |
| 갑자기 끊김 | 실행 중인 컴퓨터가 절전·종료됨 | 서버로 올리면 해결됩니다 |

## 5. 결과 읽는 법

맨 위 **결함 건수**가 결론입니다. 에러와 무감을 합한 수입니다.

그 아래 **검사 스트립**은 검사한 모든 요소를 순서대로 늘어놓은 것입니다.
빨강·주황이 몰려 있는 구간이 있다면 그 화면에 문제가 집중돼 있다는 뜻입니다.
눈금을 클릭하면 해당 항목이 열립니다.

목록은 페이지별로 묶이고, 각 묶음 안에서 **심각한 것부터** 나옵니다.
항목을 클릭하면 오른쪽에 상세가 열립니다.

- **묶음 제목을 클릭하면 접거나 펼칠 수 있습니다.** `모두 접기` 로 한 번에 정리할 수도 있습니다.
- 결과가 많으면 `한 번에` 에서 표시 개수(20·50·100·전체)를 고르고 쪽을 넘겨 봅니다.
  접어 둔 묶음의 항목은 쪽 수에서 빠지지만, 펼치면 그대로 다시 나옵니다.

- **판정 근거** — 왜 그렇게 판정했는지 (예: `HTTP 500 — api.example.com/export`)
- **클릭한 뒤 관찰된 것** — 다섯 신호 각각의 유무
- **결함 발생 화면** — 에러·무감일 때 그 시점 스크린샷
- **이슈로 복사** — 이슈 티켓에 바로 붙여넣을 형식으로 복사

### 무감이 나왔을 때

먼저 **직접 눌러보세요.** 판정이 항상 맞지는 않습니다.

진짜 무감이라면 대개 셋 중 하나입니다.

1. **핸들러 연결 누락** — 가장 흔합니다. 개발자에게 전달하면 됩니다.
2. **조건 분기에 걸림** — 권한이나 데이터 상태 때문에 아무 동작도 하지 않는 경우입니다.
   의도된 것이라면 결함이 아닙니다.
3. **의도적으로 비활성** — 이 경우는 `disabled` 처리를 권합니다.
   그러면 다음 검사부터 제외로 분류되고, 사용자도 누를 수 없다는 걸 알 수 있습니다.

### 판정 근거 읽기

목록의 오른쪽에 왜 그렇게 판정했는지가 적힙니다. 자주 나오는 것들입니다.

**클릭하지 못한 경우**

| 문구 | 뜻 | 확인할 것 |
|---|---|---|
| `<div class="…"> 가 위를 덮고 있어 클릭이 닿지 않습니다` | 다른 요소가 가림 | 모달·오버레이가 떠 있는 상태였는지. 실제 사용자도 못 누르는 상황일 수 있습니다 |
| 요소가 계속 움직여서 클릭하지 못했습니다 | 애니메이션·스크롤 중 | 대개 문제없습니다. 관찰 시간을 늘려보세요 |
| 화면에 보이지 않는 상태라 클릭하지 못했습니다 | 숨겨진 요소 | 접힌 메뉴 안의 항목일 수 있습니다 |
| 클릭하려는 순간 요소가 화면에서 사라졌습니다 | 목록이 새로 그려짐 | 실시간 갱신되는 화면에서 흔합니다 |
| 2.5초 안에 클릭할 수 없었습니다 | 원인이 특정되지 않음 | 직접 눌러보세요 |

**페이지를 열지 못한 경우**

| 문구 | 뜻 |
|---|---|
| 서버가 연결을 거부했습니다 | 대상 서버가 꺼져 있거나 포트가 다릅니다 |
| 주소를 찾을 수 없습니다 | 주소 오타이거나 사내망·VPN에 연결돼 있지 않습니다 |
| 인증서를 신뢰할 수 없습니다 | 설정에서 `인증서 경고를 넘기고 검사` 를 켜보세요 |
| 정해진 시간 안에 페이지가 열리지 않았습니다 | 서버가 느리거나 응답이 없습니다 |
| 다른 이동이 끼어들어 중단됐습니다 | 검사 과정에서 생긴 것이라 결함으로 세지 않습니다 |

### 탭으로 화면을 나눈 곳

탭을 눌러 내용만 바뀌는 화면(주소가 그대로인 경우)도 정상으로 판정합니다.
다만 **한 번에 한 탭만 검사할 수 있습니다.**

다른 탭 안에 있는 버튼은 화면에 없으므로 `접혀 있거나 다른 탭에 있어 보이지 않음` 으로 제외됩니다.
ER이 한 바퀴 돈 뒤 한 번 더 확인하지만, 그때도 그 탭이 닫혀 있으면 검사하지 못합니다.

**탭마다 주소가 다르면**(`?tab=order` 같은) 그 주소를 각각 검사하세요.
주소가 같다면 지금은 열려 있는 탭만 검사됩니다.

### 오탐이 의심될 때

- **반응이 2초보다 느린 버튼** — 프로젝트 설정에서 관찰 시간을 3000~5000ms로 늘려보세요.
- **정상인데 무감으로 나옴** — 화면 어디에도 변화가 없는 동작일 수 있습니다(예: 클립보드 복사만 하는 버튼).
- **비정상인데 정상으로 나옴** — 클릭과 무관한 배너·광고가 같은 순간에 바뀌었을 수 있습니다.

---

## 6. 프로젝트

검사 대상 시스템마다 **프로젝트**를 하나씩 만들어 씁니다. 프로젝트마다 따로 저장되는 것은 셋입니다.

| 항목 | 내용 |
|---|---|
| 검사할 주소 | 자주 검사하는 주소 목록. 새 검사 화면에서 눌러서 고릅니다 |
| 로그인 | 로그인 없음 / 아이디·비밀번호 / 직접 로그인 중 하나와 그 계정·세션 |
| 규칙 | 클릭하지 않을 단어, 관찰 시간, 최대 페이지 수, 검사 범위 등 |

그래서 A 시스템의 계정을 저장해도 B 시스템의 계정이 덮어써지지 않고,
관리자 화면에는 `안전 우선`, 개발 서버에는 `최소` 처럼 규칙을 따로 둘 수 있습니다.
**검사 이력과 "지난 검사와 비교" 도 프로젝트별로 나뉩니다.**

- **만들기** — 왼쪽 `프로젝트` 옆 `＋` 를 누르고 이름을 입력합니다. 지금 프로젝트의 규칙을 복사해 만들어집니다.
- **고치기** — 프로젝트를 고른 뒤 왼쪽 메뉴의 `프로젝트 설정`. 이름·주소·로그인·규칙을 고치고 `저장` 을 누릅니다.
- **지우기** — `프로젝트 설정` 맨 아래 `프로젝트 삭제`. 저장된 계정·로그인도 함께 지워지고 검사 기록은 남습니다.
- `빠른 검사` 는 지울 수 없고 이름도 바꿀 수 없습니다.
- 마지막으로 고른 프로젝트는 브라우저가 기억합니다. 다음에 접속하면 그 프로젝트로 열립니다.

새 검사 카드의 `＋ 세부 설정` 에서 바꾼 값은 **그 검사에만** 쓰입니다.
계속 쓰려면 `프로젝트 설정` 에서 저장하세요.

> 프로젝트가 생기기 전 버전을 쓰셨다면, 처음 실행할 때 예전 설정이 자동으로 옮겨집니다.
> `config.local.json` 의 규칙은 `빠른 검사` 로, 저장해 둔 계정·로그인은 로그인 주소의 호스트 이름으로 된 프로젝트로 들어갑니다.
> 원래 파일은 `.migrated` 를 붙여 남겨 둡니다.

### 클릭하지 않을 단어 — 빠른 설정

| 설정 | 막는 범위 | 언제 |
|---|---|---|
| **기본** | 삭제·탈퇴·결제·로그아웃·구매·주문 | 일반적인 화면 |
| **안전 우선** | 기본 + 발송·전송·승인·반려·확정·정산·환불·마감·배포·복구·동기화 | 운영·관리자 화면 |
| **최소** | 삭제·결제만 | 개발 환경에서 더 많이 검사하고 싶을 때 |

### 조정할 수 있는 값

| 항목 | 설명 | 기본값 |
|---|---|---|
| 요소당 관찰 시간 | 클릭 후 변화를 기다리는 최대 시간. 반응이 감지되면 더 기다리지 않습니다 | 2000ms |
| 최대 페이지 수 | 순회를 멈출 기준. 큰 사이트에서 무한정 도는 것을 막습니다 | 50 |
| 클릭하지 않을 단어 | 이 단어가 들어간 버튼은 클릭하지 않습니다 (쉼표로 구분) | 삭제, 탈퇴, 결제, 로그아웃, 구매, 주문 |
| 새 창 링크도 검사 | 새 창으로 열리는 링크를 눌러봅니다. 창이 열리면 바로 닫고 "새 창 열림"으로 기록합니다 | 꺼짐 |
| 외부 링크도 검사 | 다른 사이트로 나가는 링크까지 눌러봅니다 | 꺼짐 |

단어는 보이는 글자뿐 아니라 `aria-label`·`title`·`id`·`class` 까지 훑어 **부분 일치**로 찾습니다.
아이콘만 있어 글자가 없는 삭제 버튼도 걸러집니다.

**클릭하지 않을 단어는 검사 전에 꼭 확인하세요.** 운영 환경을 검사한다면 특히 중요합니다.
`발송`, `승인`, `반려`, `확정` 처럼 되돌릴 수 없는 동작이 있다면 추가하시길 권합니다.

새 프로젝트를 만들거나 `규칙을 기본값으로` 를 누를 때 쓰는 처음 값은 `config.json` 에 있습니다.

```json
{
  "observeMs": 2000,
  "maxPages": 50,
  "scope": "path",
  "ignoreHTTPSErrors": true,
  "excludeRules": ["삭제", "탈퇴", "결제", "로그아웃", "구매", "주문"]
}
```

`ignoreHTTPSErrors` 는 개발 서버의 자체 서명 인증서 경고를 무시하는 설정입니다.

---

## 7. 로그인이 필요한 화면

`프로젝트 설정` → `로그인` 에서 방식을 고릅니다. 이 프로젝트로 검사할 때마다 그 방식으로 로그인한 뒤 검사합니다.

### 방법 1 — 아이디·비밀번호 (OTP 포함)

로그인 주소·아이디·비밀번호를 넣습니다. **OTP 칸이 있는 화면도 됩니다.**

`OTP` 칸에는 **실제로 넣을 값을 그대로** 적으면 됩니다.

| 상황 | OTP 칸에 넣을 값 | 동작 |
|---|---|---|
| 개발 서버에 통하는 고정 코드가 있다 | `123123` 처럼 그 값 | 매번 그 값을 그대로 입력 |
| 인증 앱(Authenticator)을 쓴다 | 앱 등록 화면의 **비밀키** | 검사할 때마다 그 시각의 코드를 만들어 입력 |
| 문자·이메일로 OTP를 받는다 | — | 이 방법으로는 안 됩니다. 방법 2를 쓰세요 |

`입력될 값 미리보기` 를 누르면 실제로 어떤 값이 입력될지 미리 볼 수 있습니다.
비밀키를 넣었다면 지금 코드가 나오니 인증 앱 화면과 같은지 대조해 보세요.

**`저장`** 을 누르면 프로젝트에 저장됩니다.
비밀번호와 OTP 값은 저장은 되지만 **화면에는 다시 표시되지 않습니다.**
서버는 사내망에 열려 있으므로, 접속만 하면 값을 볼 수 있으면 안 되기 때문입니다.
바꿀 때만 새로 입력하면 되고, 비워 두고 저장하면 기존 값을 그대로 씁니다.
지우려면 `저장된 비밀번호 지우기` · `저장된 OTP 지우기` 를 누르세요.

### 방법 2 — 직접 로그인해서 상태 저장

**`로그인 창 열기`** 를 누르면 브라우저 창이 **서버가 실행 중인 컴퓨터 화면**에 뜹니다.
직접 로그인하고 — SSO든 문자 OTP든 무엇이든 — 원하는 화면이 보이면
**`로그인 다 했어요`** 를 누릅니다. 그 로그인 상태가 **이 프로젝트에** 저장되어 이후 검사에 쓰입니다.

세션이 만료되면 `다시 로그인` 을 누르면 됩니다.

> 이 방법은 **화면이 있는 컴퓨터에서 서버를 실행할 때만** 쓸 수 있습니다. 창이 그 컴퓨터 화면에 뜨기 때문입니다.
> 사내 서버(화면 없음)에서는 ER이 이를 감지해 막고 안내합니다. 그 경우 방법 1을 쓰세요.

터미널에서 하려면:

```bash
npm run login -- https://dev-admin.example.co.kr/ --project "bhc 관리자"   # 저장
node scan.js --project "bhc 관리자" --session --scope page
```

`--project` 를 빼면 `빠른 검사` 프로젝트에 저장됩니다.

> 저장 파일(`data/projects.json`, `data/sessions/`)에는 계정 정보와 로그인 쿠키가 들어 있습니다.
> **git에 올라가지 않도록 제외돼 있으며, 다른 사람에게 공유하면 안 됩니다.**

### ⚠️ 저장된 로그인은 접속한 누구나 쓸 수 있습니다

계정 정보나 로그인 세션을 서버에 저장하면, **접속한 다른 사람도 그 계정으로 검사를 실행할 수 있습니다.**

비밀번호·OTP 값이나 세션 쿠키가 화면에 표시되지는 않습니다. 하지만 검사를 돌리면
**그 계정 권한으로 대상 화면의 버튼을 클릭합니다.** 값을 못 봐도 쓸 수는 있는 셈입니다.

- **운영 계정은 쓰지 마세요.** 검사 전용 계정을 따로 만드시길 권합니다.
- 필요 없어지면 `프로젝트 설정` → `로그인` 에서 지우거나, 프로젝트째 지우세요.

## 8. 명령줄로 쓰기

화면 없이 터미널에서 바로 돌릴 수 있습니다.

```bash
node scan.js https://example.com
node scan.js http://localhost:8080 --scope domain --observe 3000
node scan.js https://example.com --exclude "삭제,결제,발송" --max-pages 20
node scan.js --project "bhc 관리자"            # 프로젝트의 첫 번째 주소를 그 프로젝트 설정으로
node scan.js --projects                        # 프로젝트 목록
```

옵션을 주지 않은 값은 프로젝트(기본: `빠른 검사`)의 규칙을 따르고, 로그인도 프로젝트 설정대로 합니다.

| 옵션 | 설명 |
|---|---|
| `--project <이름\|ID>` | 이 프로젝트의 규칙·로그인으로 검사. 주소를 생략하면 첫 번째 등록 주소 |
| `--projects` | 프로젝트 목록 보기 |
| `--scope page\|path\|domain` | 검사 범위 |
| `--observe <ms>` | 요소당 관찰 시간 |
| `--max-pages <n>` | 최대 페이지 수 |
| `--exclude "단어,단어"` | 클릭하지 않을 단어 |
| `--session` | 프로젝트에 저장된 직접 로그인 사용 |
| `--no-login` | 프로젝트에 로그인 설정이 있어도 로그인하지 않음 |
| `--click-new-tab` · `--click-external` | 새 창 링크 · 외부 링크도 검사 |
| `--out <경로>` | 리포트 저장 위치 |

리포트는 `report/` 에 HTML·엑셀로 저장됩니다.

**결함이 있으면 종료 코드 1로 끝납니다.** 배포 파이프라인에서 게이트로 쓸 수 있습니다.

```bash
node scan.js https://staging.example.com --scope page || echo "결함 발견 — 배포 중단"
```

---

## 9. 리포트

검사가 끝나면 네 가지로 받을 수 있습니다.

- **엑셀** — 요약 시트와 상세 시트. 이슈 티켓 첨부나 공유용입니다. 자동 필터가 걸려 있습니다.
- **Markdown** — 이슈 티켓·위키·메신저에 그대로 붙여넣는 형식입니다.
- **리포트 열기** — 단일 HTML 파일. 브라우저에서 열리고 인쇄하면 PDF로 저장됩니다.
- **인쇄** — 현재 화면을 그대로 인쇄합니다.

### 탭별로 내보내기

**내보내기는 지금 보고 있는 탭을 따릅니다.** 무감 탭에서 엑셀을 받으면 무감 항목만 담깁니다.
버튼 왼쪽에 `무감 기준` 처럼 어떤 범위인지 표시되니 확인하고 받으세요.

### 다시 검사

결과 화면의 **`같은 설정으로`** 를 누르면 같은 주소와 설정을 새 검사 폼에 불러옵니다.
**검사는 바로 시작하지 않습니다.** 설정을 확인한 뒤 `검사 시작` 을 직접 누르세요.
검사 기록은 첫 화면의 **최근 검사** 목록에 남고, 거기서 지난 리포트를 열거나 설정을 불러올 수 있습니다.

기록은 **서버**의 `reports/history.json` 에 **1년간** 보관됩니다.
브라우저가 아니라 서버에 남으므로, 접속한 모든 사람이 같은 목록을 봅니다.
1년이 지난 기록은 리포트 파일째 자동으로 정리됩니다.

왼쪽 메뉴의 **검사 이력** 에서 **지금 고른 프로젝트의** 전체 기록을 봅니다. 검사 범위, 판정 분포, 소요 시간까지 나오고
거기서 바로 리포트를 열거나 재검사할 수 있습니다.

리포트 파일은 `reports/<검사ID>/` 에 저장됩니다.
스크린샷은 그 아래 `shots/` 에 있으므로, **폴더째 공유해야 이미지까지 보입니다.**

---

## 10. 자주 겪는 문제

**로그인이 안 되고 "아이디 입력칸을 찾지 못했습니다" 라고 나옴**
로그인 주소가 실제 로그인 화면이 맞는지 확인하세요. 이미 로그인된 상태로 리다이렉트되는 주소일 수 있습니다.

**"OTP 입력을 요구합니다" 라고 나옴**
OTP 칸에 넣을 값을 지정하지 않았습니다. 위 7번을 보세요.

**`npm start` 를 했는데 "브라우저가 설치되어 있지 않습니다"**
```bash
npx playwright install chromium
```

**`EADDRINUSE: address already in use` 라고 나옴**

3000번 포트를 이미 다른 프로그램이 쓰고 있다는 뜻입니다. 대개 **ER 서버가 이미 떠 있는** 경우예요.

```bash
# 이미 떠 있는 서버를 그대로 쓰기
open http://localhost:3000

# 기존 서버를 정리하고 다시 시작하기
lsof -ti tcp:3000 | xargs kill
npm start

# 다른 포트로 시작하기
PORT=4000 npm start
```

ER은 이 상황에서 위 방법들을 안내해줍니다.

**검사가 너무 오래 걸림**
`이 페이지만` 으로 범위를 좁히거나, 최대 페이지 수를 줄이세요. 관찰 시간을 1000ms로 낮추는 것도 방법입니다.

**모든 요소가 "클릭 불가"로 나옴**
로그인이 필요한 화면인데 로그인 없이 검사했거나, 모달이 화면을 덮고 있을 가능성이 큽니다.

**결과가 하나도 없음**
로그인 페이지로 튕겼거나, 화면이 JS로 늦게 그려져 요소 수집 시점에 아직 비어 있었을 수 있습니다.

**사내 개발 서버에서 인증서 경고로 진입 실패**
`config.json` 의 `ignoreHTTPSErrors` 가 `true` 인지 확인하세요.

---

## 11. 검사 전 확인할 것

- [ ] **운영 환경을 검사한다면** 클릭하지 않을 단어에 되돌릴 수 없는 동작을 모두 넣었는가
- [ ] 검사용 계정을 쓰고 있는가 (운영 계정 아님)
- [ ] 검사 대상이 다른 사람이 쓰고 있는 서버는 아닌가 (클릭이 실제로 실행됩니다)

### 결제(PG) 화면을 검사한다면

결제창은 대개 **새 창으로** 뜹니다. `세부 설정` → **`새 창 링크도 검사`** 를 켜면
창이 열리는지 확인합니다. 창은 열리자마자 닫고 "새 창 열림 — (주소)" 로 기록하므로
결제가 진행되지는 않습니다.

주의할 점이 있습니다.

- 결제 버튼 이름에 `결제` 가 들어가면 **제외 단어에 걸려 검사되지 않습니다.**
  검증하려면 제외 단어에서 빼야 하는데, 그러면 **실제 결제 흐름이 시작될 수 있습니다.**
- 그래서 **테스트 PG 환경에서만** 하시고, 운영 PG 키가 물린 환경에서는 제외 단어를 그대로 두세요.
- 창이 열린 뒤의 화면(카드 입력 등)까지는 검사하지 않습니다. 창이 떴는지까지만 봅니다.

### 관리자 시스템을 검사한다면

관리자 화면에는 되돌릴 수 없는 버튼이 많습니다. 처음에는 이렇게 시작하세요.

1. 범위를 **`이 페이지만`** 으로 두고 한 화면씩
2. 클릭하지 않을 단어를 넉넉하게 — 예시:

```
삭제, 탈퇴, 결제, 로그아웃, 구매, 주문, 발송, 전송, 승인, 반려, 확정, 정산, 취소, 환불, 마감, 배포
```

3. 결과를 보고 안전하다고 판단되면 범위를 넓히기

**운영 서버에서는 쓰지 마세요.** 개발·스테이징 환경 전용으로 생각하시는 편이 안전합니다.

이 도구는 **버튼을 실제로 클릭합니다.** 제외 규칙에 걸리지 않은 버튼은 모두 눌립니다.
처음 돌릴 때는 개발·스테이징 환경에서 시작하시길 권합니다.

# 자동 스모크 테스트 플랫폼 기능정의서

**문서 버전** v0.1
**연관 문서** 기획안_자동스모크테스트.md

---

## 1. 기능 목록 요약

| ID | 기능명 | 우선순위 | 비고 |
|------|--------|:--------:|------|
| F-01 | 대상 등록 (URL 입력) | P0 | 필수 |
| F-02 | 인증 정보 입력 | P1 | 선택 |
| F-03 | 요소 수집 (크롤링) | P0 | 필수 |
| F-04 | 클릭 동작 실행 | P0 | 필수 |
| F-05 | 변화 신호 감지 | P0 | 무감 판정 핵심 |
| F-06 | 결함 3단계 분류 | P0 | 필수 |
| F-07 | 에러 부가 점검 | P1 | SSL·링크·이미지 |
| F-08 | 제외 규칙 설정 | P1 | 파괴적 동작 보호 |
| F-09 | 리포트 출력 (리스트) | P0 | 필수 |
| F-10 | 대시보드 (Pass/Fail율 시각화) | P0 | 필수 |
| F-11 | 결과 Export (HTML/Excel/PDF) | P1 | 내보내기 |
| F-12 | 재검사 이력 비교 | P2 | 확장 |
| F-13 | 스케줄/알림 | P2 | 확장 |

---

## 2. 기능 상세

### F-01 대상 등록 (URL 입력)
- **입력**: 운영 URL (필수), 스캔 범위(단일 페이지 / 도메인 하위 전체)
- **처리**: URL 유효성·접근성 확인, HTTP 상태 코드 확인
- **출력**: 등록 성공/실패, 최초 응답 코드
- **예외**: 접근 불가(타임아웃, DNS 실패) 시 사유 표시

### F-02 인증 정보 입력
- **입력**: 아이디/비밀번호 또는 로그인 시나리오(단계 정의), 로그인 페이지 URL
- **처리**: 로그인 폼 자동 입력 → 제출 → 세션 확보 여부 확인
- **출력**: 로그인 성공/실패, 세션 유지 상태
- **예외**: 로그인 실패 시 스캔 중단하고 사유 표시. 2FA 감지 시 수동 개입 안내
- **비고**: 자격 정보는 세션 한정 사용, 저장 정책은 구현 시 별도 결정

### F-03 요소 수집 (크롤링)
- **입력**: 등록된 URL, 스캔 범위
- **처리**: 페이지 렌더링 후 clickable 요소 수집 (button, a, role=button, onClick 바인딩 요소 등)
- **출력**: 검사 대상 요소 목록 (선택자, 라벨, 위치)
- **비고**: disabled·aria-disabled·hidden 요소는 목록에 표시하되 "검사 제외"로 분류

### F-04 클릭 동작 실행
- **입력**: 검사 대상 요소 목록
- **처리**: 요소별로 클릭 이벤트 실행, 클릭 직전 상태 스냅샷 저장
- **출력**: 각 클릭의 실행 성공 여부
- **예외**: 클릭 불가(요소 가려짐, 이탈) 시 "클릭 불가"로 기록

### F-05 변화 신호 감지 (무감 판정 핵심)
- **입력**: 클릭 직전 상태 스냅샷
- **처리**: 클릭 후 관찰 대기시간 동안 아래 신호 관찰
  - DOM 변화 (MutationObserver)
  - 네트워크 요청 발생 여부
  - URL/라우팅 변화
  - 콘솔 로그 출력
  - 요소 상태·포커스·클래스 변화
  - 스크린샷 픽셀 diff
- **출력**: 감지된 신호 목록 (없으면 빈 목록)
- **파라미터**: 관찰 대기시간(기본값 설정, 조정 가능)

### F-06 결함 3단계 분류
- **입력**: F-05의 감지 신호, F-04의 실행 결과, 콘솔·네트워크 로그
- **처리 규칙**:
  - 콘솔 예외 또는 네트워크 4xx/5xx 발생 → **ERROR**
  - 실행됐으나 감지 신호 0개 → **NO-RESPONSE(무감)**
  - 감지 신호 1개 이상 → **PASS**
- **출력**: 요소별 분류 결과

### F-07 에러 부가 점검
- **처리**: SSL 인증서 만료일, 깨진 링크(404), 로드 실패 이미지, 페이지 진입 시 콘솔 에러 수집
- **출력**: 부가 점검 결과 목록

### F-08 제외 규칙 설정
- **입력**: 제외할 선택자/텍스트 패턴 (예: "삭제", "결제", "탈퇴")
- **처리**: 매칭 요소를 클릭 대상에서 제외
- **출력**: 제외된 요소 목록
- **목적**: 파괴적·비가역 동작 오클릭 방지

### F-09 리포트 출력 (리스트)
- **처리**: 스캔 결과를 요소별 리스트로 페이지 내 표시
- **출력 항목**: 요소별 라벨·위치·분류·감지 신호·스크린샷·재현 정보
- **기능**: 분류(ERROR/NO-RESPONSE/PASS/EXCLUDED) 필터, 정렬, 검색

### F-10 대시보드 (Pass/Fail율 시각화)
- **처리**: 스캔 결과를 집계해 상단 대시보드로 시각화
- **출력 항목**:
  - Pass율 / Fail율 (ERROR+NO-RESPONSE 기준)
  - 분류별 건수 (ERROR / NO-RESPONSE / PASS / EXCLUDED)
  - 분류 비율 도넛/바 차트
  - (확장 시) 재검사 회차별 Pass율 추이
- **목적**: 리스트를 열기 전 결함 규모를 한눈에 파악

### F-11 결과 Export (HTML/Excel/PDF)
- **입력**: 현재 스캔 결과, Export 형식 선택
- **처리**:
  - HTML: 대시보드+리스트 포함 단일 파일
  - Excel: 요소별 결과 표 (분류·신호·위치 등 컬럼), 요약 시트 포함
  - PDF: 대시보드 요약 + 리스트 인쇄용 레이아웃
- **출력**: 선택 형식의 파일 다운로드
- **비고**: Excel은 QA 리포트 공유·이슈 티켓 첨부용, PDF는 보고용

### F-12 재검사 이력 비교
- **처리**: 동일 URL 재스캔 시 직전 결과와 비교
- **출력**: 신규 발생 결함 / 해소된 결함 / 유지 결함

### F-13 스케줄/알림
- **처리**: 주기적 자동 재검사, 신규 결함 발생 시 알림 발송
- **출력**: Slack/메일 알림, 대시보드 갱신

---

## 3. 상태 정의 (요소별)

| 상태 | 의미 |
|------|------|
| PASS | 클릭 후 유효한 변화 신호 감지 |
| ERROR | 클릭·진입 시 에러 발생 |
| NO-RESPONSE | 클릭됐으나 모든 신호 무변화 (무감) |
| EXCLUDED | 제외 규칙 또는 disabled로 검사 제외 |
| UNCLICKABLE | 요소가 가려지거나 이탈해 클릭 불가 |

## 4. 오탐 방지 규칙

- disabled / aria-disabled / 화면 밖 요소는 EXCLUDED로 사전 분류
- 비동기 반응 대비 관찰 대기시간 확보
- 동적 노이즈(광고·A/B·시계 등)는 재검사 baseline 비교로 필터
- 이미 활성 상태인 요소(선택된 탭) 재클릭은 무감 판정에서 완화 처리

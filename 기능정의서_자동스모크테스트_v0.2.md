# 자동 스모크 테스트 플랫폼 기능정의서

**문서 버전** v0.2 (2026-09-18, 현재 구현 기준으로 갱신)
**연관 문서** 기획안_자동스모크테스트_v0.2.md · MANUAL.md(사용 설명서)

### 변경 이력

| 버전 | 날짜 | 내용 |
|------|------|------|
| v0.1 | — | 최초 기능 정의 (F-01 ~ F-13) |
| v0.2 | 2026-09-18 | 구현 결과 반영. 기능별 구현 상태 표기, F-14 프로젝트 관리 · F-15 검사 진행 표시 · F-16 CLI · F-17 공유 접속 추가, F-02·F-05·F-10·F-11·F-12 를 실제 동작 기준으로 수정 |

**구현 상태 표기** ✅ 구현 · 🟡 부분 구현 · ⬜ 미구현

---

## 1. 기능 목록 요약

| ID | 기능명 | 우선순위 | 상태 | 비고 |
|------|--------|:--------:|:----:|------|
| F-01 | 대상 등록 (URL 입력) | P0 | ✅ | 프로젝트 등록 주소 선택 포함 |
| F-02 | 인증 (로그인) | P1 | ✅ | 계정+OTP / 직접 로그인 세션, 프로젝트별 저장 |
| F-03 | 요소 수집 (크롤링) | P0 | ✅ | 페이지 순회, 렌더링 완료 대기 |
| F-04 | 클릭 동작 실행 | P0 | ✅ | 이탈 시 원래 화면 복귀 |
| F-05 | 변화 신호 감지 | P0 | ✅ | 노이즈 기준선·적응형 관찰 |
| F-06 | 결함 분류 | P0 | ✅ | ERROR / NO-RESPONSE / PASS / EXCLUDED / UNCLICKABLE |
| F-07 | 에러 부가 점검 | P1 | 🟡 | 페이지 진입 실패·요청 실패만. SSL 만료·깨진 이미지 미구현 |
| F-08 | 제외 규칙 설정 | P1 | ✅ | 제외 단어 + 빠른 설정, 프로젝트별 |
| F-09 | 리포트 출력 (리스트) | P0 | ✅ | 페이지별 묶음, 필터·검색·쪽 나눔 |
| F-10 | 대시보드 (결과 요약 시각화) | P0 | ✅ | 결함 건수·검사 스트립·분포·추이 |
| F-11 | 결과 Export (Excel/Markdown/HTML·PDF) | P1 | ✅ | 보고 있는 탭 기준 내보내기 |
| F-12 | 재검사 · 이력 비교 | P2 | 🟡 | 지표 비교만. 요소 단위 신규/해소 비교 미구현 |
| F-13 | 스케줄/알림 | P2 | ⬜ | |
| F-14 | 프로젝트 관리 | P0 | ✅ | v0.2 신규 |
| F-15 | 검사 진행 표시 · 중단 | P1 | ✅ | v0.2 신규 |
| F-16 | 명령줄 실행 (CLI) | P1 | ✅ | v0.2 신규 |
| F-17 | 공유 접속 · 실행 환경 안내 | P1 | ✅ | v0.2 신규 |

---

## 2. 기능 상세

### F-01 대상 등록 (URL 입력) ✅
- **입력**: 검사할 주소(필수, `http://`·`https://`), 검사 범위(이 페이지만 / 하위 경로까지 / 사이트 전체)
- **처리**
  - 프로젝트에 등록된 주소가 있으면 입력칸 아래 칩으로 표시, 눌러서 선택
  - 입력칸 아래 요약 줄에 이 검사에 적용될 로그인 방식·제외 단어 수·관찰 시간·최대 페이지 수 표시
  - `세부 설정`에서 이번 검사에만 쓸 값을 바꿀 수 있음 (프로젝트 값은 그대로)
  - 제외 단어가 비어 있으면 시작 전 확인 창
  - 로그인 설정이 불완전하면(계정 누락·저장된 로그인 없음) 시작하지 않고 프로젝트 설정으로 안내
- **예외**: 주소 형식 오류 시 거부. 접근 불가(연결 거부·DNS·인증서·타임아웃)는 페이지 진입 에러로 기록하고 사유를 쉬운 말로 표시

### F-02 인증 (로그인) ✅
- **입력**: 프로젝트 설정 → 로그인 방식 선택
  | 방식 | 입력 | 동작 |
  |------|------|------|
  | 로그인 없음 | — | 그대로 검사 |
  | 아이디·비밀번호 | 로그인 주소, 아이디, 비밀번호, OTP(선택) | 검사 시작 시 로그인 폼 자동 입력·제출 |
  | 직접 로그인 | 로그인 주소 | 서버 화면에 브라우저 창을 띄워 사람이 로그인 → 로그인 상태(쿠키) 저장 → 이후 검사에 사용 |
- **OTP**: 고정 코드는 그대로 입력, 인증 앱 비밀키는 검사 시각의 TOTP 코드를 생성해 입력. `입력될 값 미리보기`로 확인 가능
- **처리**: 로그인 성공 후 검사 진행. 첫 페이지가 로그인 화면으로 보이면 경고 로그
- **예외**: 로그인 실패·입력칸 미발견·OTP 요구 시 검사 중단하고 사유 표시. 화면 없는 서버에서는 직접 로그인 방식을 막고 대안 안내
- **저장 정책 (v0.1 미정 → 확정)**
  - 프로젝트별로 서버에 저장 (`data/projects.json`, `data/sessions/<프로젝트ID>.json`, git 제외)
  - 비밀번호·OTP 값은 화면에 되돌려주지 않고 저장 여부만 표시. 빈 값으로 저장하면 기존 값 유지, 명시적으로 지우기 가능
  - 계정 검사 시 로그에는 비밀번호를 남기지 않음
  - 저장 시 "접속한 다른 사람도 이 계정으로 검사 가능 — 검사 전용 계정 사용" 경고

### F-03 요소 수집 (크롤링) ✅
- **입력**: 시작 주소, 검사 범위, 최대 페이지 수(기본 50)
- **처리**
  - 페이지 진입 후 클릭 가능한 요소 수가 더 늘지 않을 때까지 대기 (최대 10초, SPA 대응)
  - 수집 대상: `button`, `a`, `[role=button]`, `[onclick]`, `input[type=button|submit|reset]`
  - 범위 안의 링크를 대기열에 추가해 순회, 방문한 페이지는 재방문하지 않음
  - 최대 페이지 수 도달 시 중단하고 건너뛴 페이지 수 알림
- **출력**: 요소별 페이지, 라벨(글자·aria-label·title·value·id 순), 선택자

### F-04 클릭 동작 실행 ✅
- **처리**
  - 요소별 클릭 (2.5초 안에 클릭되지 않으면 클릭 불가)
  - 숨겨진 요소는 한 바퀴 뒤에 한 번 더 확인
  - 새 창·팝업은 열린 사실만 기록하고 즉시 닫음
  - 클릭으로 페이지가 바뀌면 뒤로가기 → 실패 시 주소로 재접속(최대 3회) → 화면이 다시 그려질 때까지 대기 후 이어서 검사
- **예외**: 클릭 실패 사유(가려짐·움직임·안 보임·사라짐 등)를 해석해 UNCLICKABLE 로 기록. 원래 화면으로 끝내 돌아오지 못하면 남은 요소 수와 함께 ERROR `(검사 중단)` 기록

### F-05 변화 신호 감지 (무감 판정 핵심) ✅
- **처리**
  1. 페이지마다 클릭 없이 1초 관찰 → 저절로 변하는 위치(노이즈 기준선) 기록
  2. 클릭 후 아래 신호 관찰, 노이즈 위치의 변화는 제외
     | 신호 | 감지 방법 |
     |------|-----------|
     | 화면 구조 (DOM) | MutationObserver, 변화 위치를 조상 3단계 지문으로 구분 |
     | 서버 요청 | 클릭 이후 발생한 요청 (트래킹·수집성 요청 제외) |
     | 주소 이동 | URL 변경 또는 페이지 전환 |
     | 화면 표시 | 문서 높이·본문 길이·열린 모달(dialog, role=dialog, aria-modal) 수 변화 |
     | 스크롤 | 스크롤 위치 8px 초과 이동 (앵커·맨 위로 버튼) |
     | 새 창 | 새 창·팝업 열림 |
     | JS 예외 | pageerror, console.error (네트워크 잡음 메시지 제외) |
  3. 적응형 관찰: 최소 0.4초 관찰, 신호 감지 시 0.35초 더 보고 종료, 없으면 관찰 시간까지 대기
- **v0.1 대비 변경**: 포커스 변화는 신호에서 제외(무감 버튼 오판 원인). 스크린샷 픽셀 diff는 쓰지 않고 화면 지문으로 대체
- **파라미터**: 요소당 관찰 시간 (기본 2000ms, 300~60000ms, 프로젝트별)

### F-06 결함 분류 ✅
- **처리 규칙** (위에서부터 우선)
  1. 클릭 실패 → **UNCLICKABLE**
  2. JS 예외 → **ERROR**
  3. 4xx/5xx 응답 (트래킹 요청 제외) → **ERROR**
  4. 대상 사이트 자체 요청 실패 (검사 방식이 만든 취소·중단은 제외) → **ERROR**
  5. DOM·요청·주소·화면·스크롤·새 창 신호 모두 없음 → **NO-RESPONSE**
  6. 그 외 → **PASS**
- 페이지 진입 자체가 4xx/5xx 이거나 열리지 않으면 `(페이지 진입)` **ERROR**
- **출력**: 요소별 판정과 판정 근거 문장 (예: `HTTP 500 — api.example.com/export`, `클릭 후 2.0초 동안 무변화`)

### F-07 에러 부가 점검 🟡
- **구현**: 페이지 진입 실패(응답 코드·연결·DNS·인증서·타임아웃) 기록, 클릭 중 대상 사이트 요청 실패 기록
- **미구현**: SSL 인증서 만료일, 깨진 이미지, 페이지 진입 시점의 콘솔 에러 수집

### F-08 제외 규칙 설정 ✅
- **입력**: 클릭하지 않을 단어 목록 (프로젝트별 저장, 검사마다 일시 변경 가능)
- **빠른 설정**: 기본(삭제·탈퇴·결제·로그아웃·구매·주문) / 안전 우선(기본 + 발송·전송·승인·반려·확정·정산·환불·마감·배포·복구·동기화) / 최소(삭제·결제)
- **처리**: 보이는 글자·aria-label·title·value·id·class 를 합쳐 대소문자 무시 **부분 일치**로 검사 → 일치하면 클릭하지 않고 EXCLUDED
- **자동 제외**: disabled·aria-disabled, mailto/tel/sms/ftp/file 링크, 다른 사이트 링크(등록 도메인 기준, 옵션으로 검사), 새 탭 링크(옵션으로 검사), 끝까지 보이지 않는 요소
- **출력**: 제외 사유와 함께 `검사 제외` 탭에 표시

### F-09 리포트 출력 (리스트) ✅
- **처리**: 결과를 페이지별로 묶고 묶음 안에서 심각한 것부터 정렬
- **기능**: 판정 탭(전체/에러/무감/정상/검사 제외) 필터, 검색(일치 부분 강조), 묶음 접기·펼치기·모두 접기, 한 번에 20·50·100·전체 표시와 쪽 넘김
- **상세 패널**: 판정 근거, 신호별 관찰 결과, 결함 발생 화면(에러·무감만 스크린샷, 확대·이미지 저장), 선택자 복사, 이슈 티켓 형식으로 복사

### F-10 대시보드 (결과 요약 시각화) ✅
- **검사 결과**: 결함 건수(에러+무감) 헤드라인, 요소·페이지 수·소요 시간·정상 비율, 검사 스트립(요소마다 판정 색 눈금, 클릭 시 상세), 판정 분포 막대
- **첫 화면**: 새 검사 카드, 판정 종류 안내, 최근 검사 5건, 검사 횟수·결함 합계·회차별 결함 추이(스파크라인), Runner 상태, 접속 주소
- 모두 **선택한 프로젝트 기준**

### F-11 결과 Export ✅
- **형식**
  - Excel: 요약 시트 + 상세 시트(자동 필터)
  - Markdown: 이슈 티켓·위키 붙여넣기용
  - HTML: 단일 파일 리포트. 인쇄하면 PDF로 저장 (PDF 파일 직접 생성은 하지 않음)
  - 인쇄: 현재 화면 인쇄용 레이아웃
- **처리**: 지금 보고 있는 판정 탭 기준으로 걸러 내보냄 (예: 무감 탭 → 무감 항목만). 버튼 옆에 기준 표시
- **저장**: `reports/<검사ID>/` 에 HTML·Excel·Markdown·스크린샷

### F-12 재검사 · 이력 비교 🟡
- **이력**: 검사 기록을 서버(`reports/history.json`)에 1년 보관, 1년 지난 기록은 리포트 파일째 정리. **프로젝트별로만 조회**
- **검사 이력 화면**: 주소·시각·범위·결과·판정 분포 막대·요소 수·소요 시간, 리포트·엑셀 열기, 설정 불러오기
- **재검사**: `같은 설정으로` / `설정 불러오기` → 주소·범위·관찰 시간·최대 페이지·제외 단어·링크 옵션을 새 검사 폼에 채움. **자동 시작하지 않음**
- **비교**: 같은 주소의 직전 검사 대비 결함 수·정상 비율·검사 시간 증감 표시
- **미구현**: 요소 단위 신규 발생 / 해소 / 유지 결함 비교

### F-13 스케줄/알림 ⬜
- 주기적 자동 재검사, 신규 결함 발생 시 Slack/메일 알림 — 미착수

### F-14 프로젝트 관리 ✅ (v0.2 신규)
- **목적**: 검사 대상 시스템마다 주소·로그인·규칙·이력을 분리해, 여러 시스템을 한 서버에서 섞임 없이 관리
- **프로젝트에 저장되는 것**: 이름, 검사할 주소 목록(이름·주소, 최대 50개), 로그인(F-02), 규칙(제외 단어·관찰 시간·최대 페이지 수·기본 검사 범위·새 창/외부 링크 검사·인증서 경고 무시)
- **전환**: 좌측 내비 맨 위 프로젝트 버튼(이름·로그인 방식·주소 수) → 드롭다운
  - 목록: `빠른 검사`(구분선으로 분리) / 프로젝트 목록 / `새 프로젝트`
  - 키보드(↑↓·Home·End·Enter·Esc) 조작, 바깥 클릭 시 닫힘
  - 화면 폭 900px 이하에서는 같은 버튼을 상단 바에 표시
  - 대시보드·검사 이력·프로젝트 설정이 모두 선택한 프로젝트 기준으로 바뀜
  - 마지막으로 고른 프로젝트를 브라우저에 기억
  - 검사 중에는 전환·생성 불가
- **생성**: 이름 입력 → **기본 규칙으로 시작** (다른 프로젝트 설정을 이어받지 않음) → 프로젝트 설정 화면으로 이동
- **수정**: 프로젝트 설정에서 이름·주소·로그인·규칙을 한 번에 저장. 규칙만 기본값으로 되돌리기 가능
- **삭제**: 저장된 계정·로그인 세션 함께 삭제, 검사 기록·리포트는 남김
- **빠른 검사**: 기본 프로젝트. 삭제·이름 변경 불가. 프로젝트가 생기기 전 기록은 빠른 검사로 간주
- **데이터 격리**
  - 이력 조회는 항상 한 프로젝트 기준 (프로젝트 미지정 시 빠른 검사)
  - 프로젝트 전환 시 이전 결과·이력을 즉시 비우고, 늦게 도착한 이전 프로젝트 응답은 버림
  - 검사는 서버가 프로젝트에서 직접 계정을 읽음 (비밀번호가 화면을 오가지 않음)
  - 직접 로그인 창은 연 프로젝트에만 저장 가능
- **이관**: 이전 버전 설정 파일(`config.local.json`, `credentials.local.json`, `auth.local.json`)을 처음 실행 시 자동으로 프로젝트로 옮기고 원본은 `.migrated` 로 보존

### F-15 검사 진행 표시 · 중단 ✅ (v0.2 신규)
- **처리**: 서버 이벤트를 실시간으로 받아 표시
  - 진행 눈금(요소마다 판정 색), 정상·무감·에러·제외 실시간 집계
  - 현재 페이지 주소, `n번째 페이지 · 이 페이지 a / b · 누적 c개 · 대기 dp`, 경과 시간
  - 로그 스트림 (INFO·CRAWL·PASS·WARN·FAIL 구분)
- **중단**: 중단 요청 후 2.5초 안에 멈추지 않으면 브라우저를 닫아 대기를 끊음. 그때까지의 결과로 리포트 생성
- 화면이 검사 진행에 연결되기 전에 발생한 로그도 서버가 보관했다가 재생해 유실되지 않음

### F-16 명령줄 실행 (CLI) ✅ (v0.2 신규)
- `node scan.js <주소> [옵션]` — `--project`, `--projects`, `--scope`, `--observe`, `--max-pages`, `--exclude`, `--session`, `--no-login`, `--click-new-tab`, `--click-external`, `--out`
- 옵션을 주지 않은 값은 프로젝트(기본: 빠른 검사) 규칙·로그인을 따름
- **결함이 있으면 종료 코드 1** → 배포 파이프라인 게이트로 사용
- `npm run login -- <주소> --project <이름>` 으로 직접 로그인 세션 저장

### F-17 공유 접속 · 실행 환경 안내 ✅ (v0.2 신규)
- 같은 사내망에서 접속할 수 있는 주소(IP·호스트 이름)를 화면과 터미널에 표시, 복사 버튼 제공
- 접속 중인 인원 수 표시
- Runner 상태: 브라우저 버전, 뷰포트, 프로젝트 수, 유휴/검사 중, 버전
- 프로젝트·이력·저장된 로그인은 서버에 저장되어 접속자 모두가 공유
- 포트 충돌, 브라우저 미설치, 리포트 파일 없음 등 흔한 오류는 해결 방법과 함께 안내

---

## 3. 상태 정의 (요소별)

| 상태 | 화면 표기 | 의미 |
|------|-----------|------|
| PASS | 정상 | 클릭 후 유효한 변화 신호 감지 |
| ERROR | 에러 | 클릭·진입 시 JS 예외, 4xx/5xx, 대상 사이트 요청 실패, 원래 화면 복귀 실패 |
| NO-RESPONSE | 무감 | 클릭됐으나 모든 신호 무변화 |
| EXCLUDED | 검사 제외 | 제외 단어·disabled·외부/새 탭 링크·숨김으로 클릭하지 않음 |
| UNCLICKABLE | 클릭 불가 | 가려짐·움직임·안 보임·사라짐으로 클릭이 닿지 않음 (`검사 제외` 탭에 함께 표시) |

## 4. 오탐 방지 규칙

- disabled / aria-disabled 요소는 EXCLUDED 로 사전 분류
- 숨겨진 요소는 한 바퀴 뒤 재확인 후에도 안 보이면 EXCLUDED
- 페이지별 노이즈 기준선(클릭 없이 1초 관찰)으로 시계·캐러셀·폴링 변화 제외
- 트래킹·수집성 요청(GA·GTM·광고·beacon·heartbeat 등) 제외
- 네트워크 잡음 콘솔 메시지(`net::ERR`, `Failed to load resource`, DevTools 안내 등) 제외
- 포커스 이동은 신호로 세지 않음
- 페이지 이동 후 복귀하며 취소된 요청(`ERR_ABORTED` 등)은 에러로 세지 않음
- 적응형 관찰로 느린 반응 대기 (관찰 시간 프로젝트별 조정)
- 주소가 같은 탭 UI는 열린 탭만 검사됨 (한계 — 사용 설명서에 안내)

## 5. 저장 데이터

| 위치 | 내용 | git |
|------|------|:---:|
| `data/projects.json` | 프로젝트 목록 (주소·로그인·규칙) | 제외 |
| `data/sessions/<프로젝트ID>.json` | 직접 로그인으로 저장한 세션 | 제외 |
| `reports/history.json` | 검사 이력 (프로젝트 ID 포함, 1년 보관) | 제외 |
| `reports/<검사ID>/` | HTML·Excel·Markdown 리포트, 스크린샷 | 제외 |
| `config.json` | 새 프로젝트·규칙 초기화 시 기본값, 로그인 폼 선택자 | 포함 |

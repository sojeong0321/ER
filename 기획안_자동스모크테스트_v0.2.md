# 자동 스모크 테스트 플랫폼 기획안

**문서 버전** v0.2 (2026-09-18, 현재 구현 기준으로 갱신)
**작성 목적** 대상 URL(운영·개발·사내망 포함)과 (선택적) 계정 정보만으로 웹 화면의 기본 동작 이상을 자동 탐지하는 도구 정의
**대상 독자** QA팀, 개발팀, 기획팀
**연관 문서** 기능정의서_자동스모크테스트_v0.2.md · MANUAL.md(사용 설명서)
**제품명** ER (화면 표기 `ER Console`)

### 변경 이력

| 버전 | 날짜 | 내용 |
|------|------|------|
| v0.1 | — | 최초 기획 |
| v0.2 | 2026-09-18 | 구현 결과 반영. 프로젝트 단위 관리 추가, 판정 방식(노이즈 기준선·적응형 관찰) 구체화, 로그인 방식(계정·OTP·직접 로그인) 확정, 실행 형태(웹 + CLI + Docker) 확정, 로드맵 진행 상황 갱신 |

---

## 1. 배경 및 문제 정의

개발팀이 최소한의 기본 검증(smoke test) 없이 QA에 전달하는 사례가 반복됨. 그 결과 QA 초기 단계에서 "버튼이 안 눌린다", "클릭해도 반응이 없다", "콘솔 에러가 뜬다" 같은 기초 결함을 수작업으로 걸러내는 데 시간이 소모됨.

이러한 기초 결함은 사람이 판단하기 전에 **자동으로 선(先) 검출**이 가능하다. 본 도구는 배포 직후 또는 QA 인입 전 단계에서 "최소한 이건 됐어야 한다" 수준의 결함을 자동으로 리포트하는 것을 목표로 한다.

## 2. 목표

- 대상 URL 입력만으로 화면 진입·렌더링·클릭 동작의 이상을 자동 탐지
- **에러(콘솔/네트워크)뿐 아니라 "무감(클릭해도 아무 반응 없음)" 결함까지 검출**
- QA 인입 전 개발팀이 스스로 돌려볼 수 있는 셀프 게이트 제공 (웹 화면 + 배포 파이프라인용 CLI)
- **운영·개발·사내망 URL 모두 지원** — 스캔 엔진이 실행되는 환경에서 접근 가능한 URL이면 대상이 됨 (localhost, 사내 IP, 내부 도메인, VPN 경유 개발 서버 포함)
- **검사 대상 시스템(프로젝트)별로 주소·로그인·규칙·이력을 분리 관리** — 여러 시스템을 한 서버에서 섞임 없이 검사

## 3. 비목표 (Out of Scope)

- 비즈니스 로직 정합성 판정 ("이 값이 정책상 맞는가"는 기대값 정의 없이 불가)
- 서버 내부 관제 (APM, 서버 로그, 메트릭)
- 부하/성능 테스트 (별도 도구 영역)
- "반응은 하나 잘못 반응하는" 결함 (시나리오/기대값 기반 테스트 영역)
- 화면 디자인의 시안 일치 여부
- 사용자 계정·권한 관리 (사내망 접속자는 모두 같은 권한으로 사용)

## 4. 핵심 개념: 결함 판정

### 4-1. 판정 분류

| 분류 | 정의 | 판정 방식 |
|------|------|-----------|
| **에러(ERROR)** | 클릭·진입 시 JS 예외, 4xx/5xx 응답, 대상 사이트 자체 요청 실패 | 콘솔·pageerror, 네트워크 응답 코드, 요청 실패 |
| **무감(NO-RESPONSE)** | 클릭했으나 관찰 시간 동안 모든 변화 신호가 무변화 | 클릭 전후 신호 비교 (노이즈 기준선 차감) |
| **정상(PASS)** | 클릭 후 하나 이상의 변화 신호가 관찰됨 | 아래 신호 중 1개 이상 감지 |
| 검사 제외(EXCLUDED) | disabled, 제외 단어, 외부·새 탭 링크, 숨김 요소 | 클릭 전 사전 판정 |
| 클릭 불가(UNCLICKABLE) | 가려짐·움직임·사라짐 등으로 클릭이 닿지 않음 | 클릭 실패 사유 해석 |

결함 건수는 **에러 + 무감**으로 집계한다.

### 4-2. 변화 신호

DOM 변화 · 네트워크 요청 · URL 이동 · 화면 표시(모달·토스트·문서 높이·본문 길이) · 스크롤 · 새 창 열림 · JS 예외.

### 4-3. 오탐 관리 (v0.2 에서 확정한 방식)

- **노이즈 기준선**: 페이지마다 클릭 없이 1초간 먼저 관찰해 시계·캐러셀·폴링 배너처럼 저절로 바뀌는 위치를 기록하고, 클릭 후에는 그 위치 밖의 변화만 신호로 인정
- **적응형 관찰**: 최소 0.4초는 지켜보고, 신호가 잡히면 0.35초 더 본 뒤 바로 다음 요소로. 신호가 없으면 관찰 시간(기본 2초)까지 대기
- 트래킹·수집성 요청(GA, 광고, beacon 등)과 네트워크 잡음 콘솔 메시지는 신호·에러에서 제외
- **포커스 이동은 신호로 세지 않음** — 버튼을 누르면 거의 항상 포커스가 옮겨가 무감 버튼까지 정상으로 판정되기 때문
- 숨겨진 요소는 한 바퀴 뒤에 한 번 더 확인 (탭 UI 대응)
- 링크 클릭으로 페이지가 바뀌면 원래 화면으로 복귀해 이어서 검사. 복귀가 불가능하면 남은 요소 수와 함께 에러로 기록

## 5. 실행 환경 및 접근성 조건

대상이 운영이냐 개발이냐는 제약이 아니다. **판단 기준은 "스캔 엔진이 실행되는 환경에서 그 URL이 열리는가" 하나다.** 스캔 엔진(Headless 브라우저)은 자신이 구동되는 PC/서버의 네트워크로 대상에 접속하기 때문이다.

| 실행 형태 | 개발·사내망 URL 접근 | 상태 |
|-----------|:---:|------|
| 검사자 PC에서 웹 서버 실행 (`npm start`) | 가능 | 구현. 같은 사내망 동료가 PC 주소로 접속해 공유 가능 |
| 사내 서버에 Docker로 배포 | 가능 | 구현 (`Dockerfile`). **최종 운영 형태** |
| CLI (`node scan.js`) | 가능 | 구현. 결함 시 종료 코드 1 → 배포 파이프라인 게이트 |
| 외부 클라우드 웹앱 | 불가 | 사내망·VPN 밖이라 URL 미도달 |

따라서 개발·사내망 URL이 주 대상이라면 스캔 엔진은 **사내망 내부(검사자 PC 또는 사내 서버)에서 실행**하는 구조여야 한다.

**개발 URL 대응**
- 자체 서명 SSL 인증서 경고 → HTTPS 오류 무시 옵션 (기본 켬, 프로젝트별 설정)
- 로그인·OTP·SSO 게이트 → 프로젝트별 로그인 설정으로 통과 (7-2 참고)
- 장시간 스캔 중 VPN 세션 유지 필요 (운영 가이드로 안내)

## 6. 검사 범위: 단일 페이지가 아닌 페이지 순회

입력 URL 한 장에만 머물지 않는다. 시작 URL에서 출발해 **지정된 범위 안의 연결된 화면들을 순회하며 검사**한다.

**동작 방식**
1. 시작 URL 진입 → 화면이 다 그려질 때까지 대기(클릭 가능한 요소 수가 더 늘지 않을 때까지) → clickable 요소 수집
2. 범위 안의 링크를 따라 새 페이지를 검사 대기열에 추가
3. 클릭으로 페이지가 전환되면 뒤로가기(실패 시 재접속)로 원래 페이지에 복귀 후 나머지 요소 이어서 검사
4. 방문 기록을 관리해 검사 완료 페이지는 재방문하지 않음 (중복·무한루프 방지)
5. 최대 페이지 수(기본 50)에 도달하면 멈추고 건너뛴 페이지 수를 알림

**스캔 범위(scope)** — 사용자가 선택
- 이 페이지만
- 하위 경로까지 (예: `/admin/product` → `/admin/` 하위)
- 사이트 전체 (같은 도메인)

**순회 중 보호 규칙**
- 제외 단어(삭제·탈퇴·결제·로그아웃·구매·주문 등)가 보이는 글자·aria-label·title·value·id·class 어디든 들어간 요소는 클릭하지 않음
- 외부 사이트 링크·새 탭 링크·mailto/tel 링크는 기본 제외 (옵션으로 검사 가능)
- 첫 페이지가 로그인 화면으로 보이면 "로그인되지 않은 상태로 검사 중" 경고

## 7. 사용 시나리오

### 7-0. 프로젝트 선택
1. 화면 좌측 맨 위 프로젝트 버튼으로 검사 대상 시스템(프로젝트)을 고름
2. 대시보드·검사 이력·프로젝트 설정이 모두 그 프로젝트 기준으로 바뀜
3. 주소만 넣고 바로 돌릴 때는 기본 프로젝트 `빠른 검사` 사용

### 7-1. 범용 스캔 (계정 없음)
1. 사용자가 대상 URL 입력 (프로젝트에 등록된 주소를 눌러 선택 가능)
2. 크롤러가 접근 가능한 페이지의 clickable 요소 수집
3. 각 요소 클릭 → 변화 신호 관찰 → 분류. 진행 상황이 실시간 눈금·로그로 표시되고 중간에 중단 가능
4. 리포트 출력 (결함 건수 + 상세 목록 + 결함 스크린샷)

### 7-2. 인증 스캔 (프로젝트에 로그인 설정)
1. 프로젝트 설정에서 로그인 방식 선택
   - **아이디·비밀번호**: 로그인 주소·계정 저장. OTP는 고정 코드 또는 인증 앱 비밀키(TOTP 자동 생성)
   - **직접 로그인**: 서버 화면에 뜬 브라우저 창에서 사람이 로그인(SSO·문자 OTP 포함) → 로그인 상태(쿠키) 저장
2. 검사 시 도구가 그 방식으로 로그인 → 인증 후 화면까지 스캔
3. 리포트 출력

### 7-3. 재검사
1. 결과 화면 또는 검사 이력에서 "설정 불러오기" → 지난 주소·설정이 새 검사 폼에 채워짐
2. **검사는 사람이 확인 후 직접 시작** (의도치 않은 클릭 실행 방지)
3. 같은 주소의 직전 검사와 결함 수·정상 비율·검사 시간을 비교해 표시

## 8. 산출물 (리포트)

- 요약: 결함 건수(에러+무감), 요소·페이지 수, 소요 시간, 정상 비율, 판정 분포
- 상세: 요소별 페이지·라벨·선택자·판정·판정 근거·관찰된 신호·결함 스크린샷
- 내보내기: 엑셀(요약·상세 시트) / Markdown(이슈·위키용) / HTML(단일 파일, 인쇄 시 PDF) — **보고 있는 판정 탭 기준으로 걸러서** 내보냄
- 이력: 프로젝트별로 서버에 1년 보관, 직전 검사 대비 지표 비교

## 9. 기술적 접근

- **렌더링/조작**: Headless Chromium (Playwright), 뷰포트 1440×900
- **변화 감지**: MutationObserver(DOM, 변화 위치 지문), 네트워크 이벤트, URL 감시, pageerror·콘솔 후킹, 화면 지문(문서 높이·본문 길이·모달 수), 스크롤 위치, 새 창 이벤트
- **서버**: Node.js + Express, 진행 상황은 Socket.IO로 실시간 전달
- **저장**: 파일 기반 — `data/projects.json`(프로젝트), `data/sessions/`(로그인 상태), `reports/`(리포트·이력). 계정이 담긴 `data/`는 git 제외
- **형태**: 웹 대시보드 + CLI + Docker 이미지

## 10. 리스크 및 한계

| 리스크 | 대응 (v0.2 상태) |
|--------|------|
| 무감 오탐 (의도된 무동작을 결함으로 오인) | disabled/aria-disabled 사전 제외, 적응형 관찰, 포커스 신호 제외. 관찰 시간 프로젝트별 조정 |
| 동적 요소(SPA·광고·시계) 오탐 | 페이지별 노이즈 기준선 차감, 트래킹 요청 제외 |
| 탭 UI에서 다른 탭 내용 미검사 | 숨김 요소 재확인. 주소가 같은 탭은 열린 탭만 검사됨(한계, 사용 설명서에 안내) |
| 인증·2FA 플로우 자동화 난이도 | TOTP 자동 생성 + 사람이 직접 로그인해 상태 저장하는 방식 제공 |
| 저장된 계정을 다른 접속자가 사용 가능 | 비밀번호·OTP 값은 화면에 되돌려주지 않음. 검사 전용 계정 사용을 저장 시점에 경고 |
| 파괴적 동작(삭제·결제 버튼) 클릭 위험 | 제외 단어 기본값 + 빠른 설정(기본/안전 우선/최소). 제외 단어가 비면 검사 전 확인 |
| 페이지 순회 중 무한 확장·순환 | 스캔 범위 지정, 방문 기록, 최대 페이지 수 |
| 여러 시스템 설정·이력이 섞임 | 프로젝트 단위 분리. 새 프로젝트는 기본값에서 시작, 이력 조회는 항상 한 프로젝트 기준 |
| 사내망·개발 서버 접근 (VPN/SSL/SSO) | 스캔 엔진 사내망 내부 실행, HTTPS 오류 무시·로그인 옵션 |
| 화면 없는 서버에서 직접 로그인 불가 | 서버가 감지해 해당 방식을 막고 대안(계정 방식·세션 파일 이전) 안내 |

## 11. 단계별 로드맵

| 단계 | 내용 | 상태 |
|------|------|------|
| 1단계 (PoC) | 로컬 스크립트, 단일 페이지, 클릭+에러+무감 탐지 | **완료** |
| 2단계 | 다중 페이지 순회, 로그인 지원, HTML·엑셀·Markdown 리포트 | **완료** |
| 3단계 | 사내 서버 배포형 대시보드, 이력 비교, 프로젝트 단위 관리 | **대부분 완료** — Docker 배포, 프로젝트별 이력·지표 비교 구현 |
| 3단계 잔여 | 스케줄 재검사, 알림(Slack/메일), 요소 단위 신규/해소 결함 비교 | 미착수 |
| 후속 검토 | SSL 만료·깨진 이미지 부가 점검, 좁은 화면(900px 이하) 메뉴 | 미착수 |

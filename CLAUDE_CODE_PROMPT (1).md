# Claude Code 작업 지시서: 자동 스모크 테스트 도구 구현

## 0. 컨텍스트 (먼저 읽을 것)

운영 중인 웹앱의 URL만 넣으면 화면을 순회하며 버튼·링크의 기본 동작 이상을 자동 탐지하는 QA 스모크 테스트 도구를 만든다. 단순 에러뿐 아니라 **"클릭해도 아무 반응이 없는(무감)" 결함**까지 잡는 것이 이 도구의 핵심 차별점이다.

첨부 파일을 반드시 먼저 읽고 시작할 것:
- `기획안_자동스모크테스트.md` — 목적, 범위, 실행환경 조건, 페이지 순회 방식
- `기능정의서_자동스모크테스트.md` — F-01~F-13 기능 명세 (이게 구현 스펙이다)
- `smoke-engine.js` — **이미 검증된 판정 엔진 PoC**. 이걸 기반으로 확장한다. 처음부터 새로 짜지 말 것.
- `test-target.html` — 판정 검증용 테스트 페이지
- `smoke-test-mockup.html` — 최종 UI 목업. 프론트는 이 디자인·레이아웃을 따른다.

## 1. 이미 검증된 것 (PoC에서 확인 완료 — 유지할 것)

`smoke-engine.js`의 판정 원리는 실측으로 검증됐다. 바꾸지 말고 계승할 것:
- 클릭 직전 상태 스냅샷 → 클릭 → 관찰 대기(기본 2000ms) → 5개 신호(DOM/NET/URL/CON/VIS) 변화 수집
- 판정 규칙: JS 예외 or 서버 4xx/5xx → **ERROR** / 신호 전부 무변화 → **NO-RESPONSE** / 신호 1개+ → **PASS** / disabled·제외규칙 → **EXCLUDED**
- **네트워크 실패(net::ERR 등)는 JS 코드 예외와 구분해 ERROR로 보지 않는다** (오탐 방지 — 이미 튜닝됨)
- 비동기 지연 반응은 관찰 대기시간으로 커버 (성급한 무감 판정 금지)
- Playwright 사용, 개발 서버 대응 위해 `ignoreHTTPSErrors: true`

## 2. 구현할 것 (PoC에 없는 부분 — 우선순위 순)

### P0 — 핵심
1. **페이지 순회(크롤링)**: 시작 URL에서 같은 도메인 내 링크를 따라 이동하며 검사 대상 확장. 방문 기록 관리로 중복·무한루프 방지. 스캔 범위 옵션(현재 페이지 / 경로 하위 glob / 도메인 전체). 클릭으로 URL 이동 시 원위치 복귀 후 나머지 요소 이어서 검사.
2. **로그인 세션 처리**: ID/PW 입력 → 로그인 → 세션 유지 상태로 인증 후 화면까지 스캔. 로그인 실패 시 중단하고 사유 표시. (2FA는 수동 개입 안내로 처리)
3. **결과 구조화**: 요소별로 `{page, label, selector, status, severity, signals, repro, screenshot}` 형태. 페이지별 그룹핑 가능하게.
4. **리포트 출력**: 목업과 동일한 구조 — 대시보드(Pass율, 분류 집계, 페이지별 커버리지) + 필터·검색 가능한 결과 리스트 + 요소 상세.

### P1 — 실무 필수
5. **스크린샷 캡처**: 클릭 후 상태 스크린샷 저장, 리포트에 첨부.
6. **DOM diff 정밀화**: 현재 PoC는 전체 HTML 문자열 비교라 시계·광고 등 동적 요소에 오탐 가능. 구조적 diff 또는 관심 영역 한정으로 개선. baseline 재검사 비교로 노이즈 필터.
7. **심각도 산정**: ERROR·무감에 P1/P2/P3 부여 규칙 정의.
8. **Export**: HTML / Excel(요약+상세 시트) / PDF. 목업의 export 로직 참고.
9. **제외 규칙 설정**: 텍스트·selector 패턴으로 파괴적 동작(삭제·결제 등) 클릭 제외. 설정 가능하게.

### P2 — 확장
10. 재검사 이력 비교(신규/해소/유지 결함 diff), 스케줄 실행, 알림(Slack/메일).

## 3. 기술·구조 요구사항

- **언어/런타임**: Node.js + Playwright (PoC와 동일 스택 유지)
- **실행 형태**: 1차로 CLI 스크립트(`node scan.js <URL> [옵션]`), 이후 로컬/사내서버 구동 웹 대시보드로 확장 가능한 구조로 모듈 분리
- **모듈 분리**: 크롤러 / 판정 엔진 / 리포터 / (프론트) 를 파일 단위로 분리. 판정 엔진은 PoC 로직 계승.
- **실행 환경 전제**: 사내망·개발 서버 대상이므로 스캔 엔진은 검사자 PC 또는 사내 서버에서 실행됨. 외부 의존 최소화.
- **설정 파일**: 스캔 범위·제외 규칙·관찰 대기시간·로그인 정보를 config로 분리(하드코딩 금지).

## 4. 작업 방식

- **먼저 `smoke-engine.js`를 실행해 현재 동작을 확인**한 뒤 시작할 것. `test-target.html`로 판정이 맞는지 회귀 확인.
- P0부터 순서대로. 각 기능 추가 후 `test-target.html`(및 필요시 케이스 추가)로 회귀 테스트해서 기존 판정이 깨지지 않았는지 확인.
- **오탐(false positive) 관리가 이 도구의 성패다.** 새 기능이 정상 동작을 결함으로 오인하지 않는지 항상 검증할 것.
- 파괴적 동작을 실수로 클릭하지 않도록 제외 규칙을 기본 활성화.
- 큰 리팩터링 전 계획을 먼저 제시하고 진행.

## 5. 1차 완료 기준 (Definition of Done)

- 실제 URL(로그인 필요한 다중 페이지 사이트)을 넣으면 페이지를 순회하며 전 버튼·링크를 검사하고,
- ERROR / NO-RESPONSE / PASS / EXCLUDED로 분류해,
- 페이지별로 그룹핑된 리포트(대시보드 + 리스트 + 스크린샷)를 HTML/Excel로 출력하며,
- `test-target.html` 회귀 테스트에서 오탐 0건을 유지한다.

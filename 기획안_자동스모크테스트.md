# 자동 스모크 테스트 플랫폼 기획안

**문서 버전** v0.1
**작성 목적** 대상 URL(운영·개발·사내망 포함)과 (선택적) 계정 정보만으로 웹 화면의 기본 동작 이상을 자동 탐지하는 도구 정의
**대상 독자** QA팀, 개발팀, 기획팀

---

## 1. 배경 및 문제 정의

개발팀이 최소한의 기본 검증(smoke test) 없이 QA에 전달하는 사례가 반복됨. 그 결과 QA 초기 단계에서 "버튼이 안 눌린다", "클릭해도 반응이 없다", "콘솔 에러가 뜬다" 같은 기초 결함을 수작업으로 걸러내는 데 시간이 소모됨.

이러한 기초 결함은 사람이 판단하기 전에 **자동으로 선(先) 검출**이 가능하다. 본 도구는 배포 직후 또는 QA 인입 전 단계에서 "최소한 이건 됐어야 한다" 수준의 결함을 자동으로 리포트하는 것을 목표로 한다.

## 2. 목표

- 대상 URL 입력만으로 화면 진입·렌더링·클릭 동작의 이상을 자동 탐지
- **에러(콘솔/네트워크)뿐 아니라 "무감(클릭해도 아무 반응 없음)" 결함까지 검출**
- QA 인입 전 개발팀이 스스로 돌려볼 수 있는 셀프 게이트 제공
- **운영·개발·사내망 URL 모두 지원** — 스캔 엔진이 실행되는 환경에서 접근 가능한 URL이면 대상이 됨 (localhost, 사내 IP, 내부 도메인, VPN 경유 개발 서버 포함)

## 3. 비목표 (Out of Scope)

- 비즈니스 로직 정합성 판정 ("이 값이 정책상 맞는가"는 기대값 정의 없이 불가)
- 서버 내부 관제 (APM, 서버 로그, 메트릭)
- 부하/성능 테스트 (별도 도구 영역)
- "반응은 하나 잘못 반응하는" 결함 (시나리오/기대값 기반 테스트 영역)

## 4. 핵심 개념: 결함 3단계 분류

| 분류 | 정의 | 판정 방식 |
|------|------|-----------|
| **에러(ERROR)** | 클릭·진입 시 콘솔 예외 또는 네트워크 4xx/5xx 발생 | 콘솔 로그, 네트워크 응답 코드 |
| **무감(NO-RESPONSE)** | 클릭했으나 DOM·네트워크·URL·콘솔·화면 전부 무변화 | 클릭 전후 상태 diff |
| **정상(PASS)** | 클릭 후 하나 이상의 변화 신호가 관찰됨 | 위 신호 중 1개 이상 감지 |

무감 판정은 오탐 관리가 관건이므로, disabled·aria-disabled 속성 요소는 사전 제외하고, 비동기 반응을 위한 관찰 대기시간을 둔다.

## 5. 실행 환경 및 접근성 조건

대상이 운영이냐 개발이냐는 제약이 아니다. **판단 기준은 "스캔 엔진이 실행되는 환경에서 그 URL이 열리는가" 하나다.** 스캔 엔진(Headless 브라우저)은 자신이 구동되는 PC/서버의 네트워크로 대상에 접속하기 때문이다.

| 실행 형태 | 개발·사내망 URL 접근 | 근거 |
|-----------|:---:|------|
| 로컬 스크립트 (검사자 PC에서 실행) | 가능 | 검사자 PC의 네트워크·VPN을 그대로 사용 |
| 사내 PC/서버에 배포한 웹앱 | 가능 | 실행 위치가 사내망 안이면 접근됨 |
| 외부 클라우드 웹앱 | 불가 | 외부 서버는 사내망·VPN 밖이라 URL 미도달 |

따라서 개발·사내망 URL이 주 대상이라면 스캔 엔진은 **사내망 내부(검사자 PC 또는 사내 서버)에서 실행**하는 구조여야 한다. 이는 PoC를 로컬 스크립트로 먼저 구현하는 근거이기도 하다.

**개발 URL 대응 시 추가 고려사항**
- 자체 서명 SSL 인증서 경고 → HTTPS 오류 무시 옵션 필요
- basic auth / 사내 SSO 게이트 → 통과 처리를 로그인 시나리오에 포함
- 장시간 스캔 중 VPN 세션 유지 필요

## 6. 검사 범위: 단일 페이지가 아닌 페이지 순회

입력 URL 한 장에만 머물지 않는다. 시작 URL에서 출발해 **지정된 범위 안의 연결된 화면들을 순회하며 검사**한다.

**동작 방식**
1. 시작 URL 진입 → 해당 페이지의 clickable 요소·링크 수집
2. 같은 도메인 내 링크를 따라 새 페이지로 이동, 검사 대상에 추가
3. 클릭으로 페이지가 전환되면 원래 페이지로 복귀 후 나머지 요소 이어서 검사
4. 방문 기록을 관리해 검사 완료 페이지는 재방문하지 않음 (중복·무한루프 방지)

**스캔 범위(scope) 옵션** — 사용자가 선택
- 현재 페이지만
- 지정 경로 하위만 (예: `/product/**`)
- 도메인 전체

**순회 중 보호 규칙**
- 파괴적·이탈 동작(로그아웃, 삭제, 외부 링크)은 제외 규칙으로 관리해 스캔이 중단되지 않도록 함

## 7. 사용 시나리오

### 7-1. 범용 스캔 (계정 없음)
1. 사용자가 대상 URL 입력
2. 크롤러가 접근 가능한 페이지의 clickable 요소 수집
3. 각 요소 클릭 → 변화 신호 관찰 → 3단계 분류
4. 리포트 출력 (에러/무감/정상 건수 + 상세 목록 + 스크린샷)

### 7-2. 인증 스캔 (계정 입력)
1. 사용자가 URL + 로그인 정보(또는 로그인 시나리오) 입력
2. 도구가 로그인 수행 → 세션 확보
3. 인증 후 접근 가능한 화면까지 스캔 확장
4. 리포트 출력

## 8. 예상 산출물 (리포트)

- 요약: 총 검사 요소 수 / 에러 / 무감 / 정상 건수
- 상세: 요소별 (위치, 라벨, 분류, 관찰된 신호, 스크린샷, 재현 정보)
- 이력: 동일 URL 재검사 시 이전 대비 신규/해소 결함 비교

## 9. 기술적 접근 (개요)

- **렌더링/조작**: Headless 브라우저 (Playwright)
- **변화 감지**: MutationObserver(DOM), 네트워크 인터셉트, URL 감시, 콘솔 후킹, 스크린샷 diff
- **부가 점검**: HTTP 상태, SSL 만료, 콘솔 에러, 깨진 링크/이미지
- **형태**: (1안) 로컬 실행 스크립트 → (2안) URL·계정 입력 웹 대시보드로 확장

## 10. 리스크 및 한계

| 리스크 | 대응 |
|--------|------|
| 무감 오탐 (의도된 무동작을 결함으로 오인) | disabled/aria 상태 사전 제외, 대기시간 튜닝 |
| 동적 요소(SPA·광고·A/B) 오탐 | 재검사 시 baseline 비교로 노이즈 필터 |
| 인증·2FA 플로우 자동화 난이도 | 로그인 시나리오 수동 정의 옵션 제공 |
| 파괴적 동작(삭제·결제 버튼) 클릭 위험 | 제외 규칙(selector blocklist) 설정 지원 |
| 페이지 순회 중 무한 확장·순환 | 스캔 범위 지정, 방문 기록 관리, 최대 깊이 제한 |
| 사내망·개발 서버 접근 (VPN/SSL/SSO) | 스캔 엔진 사내망 내부 실행, HTTPS 오류 무시·인증 통과 옵션 |

## 11. 단계별 로드맵 (제안)

- **1단계 (PoC)**: 로컬 스크립트, 단일 페이지, 클릭+에러+무감 탐지
- **2단계**: 다중 페이지 크롤링(순회), 로그인 지원, 리포트 HTML 출력
- **3단계**: 사내 서버 배포형 대시보드, 스케줄 재검사, 알림(Slack/메일), 이력 비교
